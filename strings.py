name='harsh'
print(name)
print(name[0])# indexing in strings
print(name[-3])# indexing from the back to the nth element
print(name[len(name)-1]) # printing the last character of an element
print(len(name))

name= name + 'er'
print(name)# concatinating 
name = 3* name
print(name)# concatinates the word by its self the number of times before the *

#slicing strings

some= 'somas'
some1= some[1:] # removing the first element of the string
print(some1)
some2=some[2:]
print(some2)# removing from the second element




